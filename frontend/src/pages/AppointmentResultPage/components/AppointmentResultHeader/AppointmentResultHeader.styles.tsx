import styled from '@emotion/styled';

const StyledTitle = styled.h1`
  font-size: 4rem;
`;

const StyledContent = styled.p`
  font-size: 2.4rem;
`;

const StyledLinkIcon = styled.img`
  width: 2.4rem;
`;

export { StyledTitle, StyledContent, StyledLinkIcon };
