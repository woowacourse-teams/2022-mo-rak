import styled from '@emotion/styled';

const StyledContainerLabel = styled.label`
  width: 100%;
  cursor: pointer;
`;

const StyledInput = styled.input`
  cursor: pointer;
  display: none;
`;

const StyledRadio = styled.img`
  width: 2rem;
  height: 2rem;
  position: absolute;
  left: 1.2rem;
  top: 1rem;
`;

const StyledLabel = styled.span`
  cursor: pointer;
  font-size: 1.6rem;
  text-align: center;
`;

export { StyledContainerLabel, StyledInput, StyledRadio, StyledLabel };
